# 小程序整改任务两步提交流程联调说明

## 1. 变更说明

小程序整改流程由原来的“提交材料、AI 判定通过后自动完成”调整为两步：

1. 提交整改图片进行 AI 判定；
2. AI 判定通过后，由用户确认并正式提交整改。

AI 判定通过不再自动完成任务。第一步完成后，整改任务和关联问题仍为 `PENDING`/`RECTIFYING`；只有第二步提交成功后，两者才变为 `COMPLETED`。

小程序旧接口 `POST /river/openapi/v1/rectification/complete` 已停止提供，请改用本文的两个新接口。后台管理端 `/river/admin/rectification/complete` 暂时不受本次调整影响。

## 2. 通用约定

Base URL、登录和刷新令牌规则沿用现有 OpenAPI V1 约定。业务请求头：

```http
Blade-Auth: bearer {accessToken}
Blade-Requested-With: BladeHttpRequest
Accept-Language: zh-CN
Content-Type: application/json
```

统一响应：

```json
{
  "code": 200,
  "success": true,
  "data": {},
  "msg": "操作成功"
}
```

所有 Long 类型 ID 在小程序中必须按字符串保存和传递，不要使用 `Number` 做计算，以免丢失精度。

## 3. 第一步：AI 判定

```http
POST /river/openapi/v1/rectification/ai-judgment
```

权限：`river_rectification_complete`。

请求：

```json
{
  "rectificationId": "1930000000000000601",
  "version": 0,
  "requestId": "01980f75-6254-7b5a-a3d3-f62a0ec42001",
  "imageRefs": [
    "https://example.com/river/rectification-1.jpg",
    "https://example.com/river/rectification-2.jpg"
  ]
}
```

| 字段              | 类型     | 必填 | 说明                               |
| ----------------- | -------- | ---: | ---------------------------------- |
| `rectificationId` | string   |   是 | 整改任务 ID                        |
| `version`         | number   |   是 | 任务详情中的最新版本               |
| `requestId`       | string   |   是 | 本次 AI 操作的幂等号，最长 64 字符 |
| `imageRefs`       | string[] |   是 | 已完成上传的整改图片引用，至少一张 |

响应 `data`：

```json
{
  "judgmentId": "1930000000000000701",
  "rectificationId": "1930000000000000601",
  "aiResult": "COMPLETED",
  "conclusion": "现场问题已整改完成",
  "aiJudgmentCount": 1,
  "version": 1,
  "canSubmit": true,
  "forceRequired": false
}
```

| 字段              | 说明                                     |
| ----------------- | ---------------------------------------- |
| `judgmentId`      | 本次 AI 判定记录 ID，第二步必须原样提交  |
| `aiResult`        | `COMPLETED`、`INCOMPLETE` 或 `FAILED`    |
| `conclusion`      | AI 判定结论或失败原因                    |
| `aiJudgmentCount` | 已记录的 AI 判定次数                     |
| `version`         | 第一步完成后的新版本，第二步必须使用此值 |
| `canSubmit`       | 是否允许正常提交整改                     |
| `forceRequired`   | 是否已达到连续三次未通过后的强制完成条件 |

前端分支：

- `COMPLETED`：展示结论并启用“提交整改”；此时任务仍未完成。
- `INCOMPLETE`：展示结论，禁用“提交整改”，允许重新拍摄并再次判定。
- `FAILED`：展示失败信息，禁用“提交整改”，允许重试。
- `forceRequired=true`：可按权限展示“强制完成”，仍不能调用普通提交。

每次新的 AI 判定都使用新的 `requestId`；同一次请求因超时或断网重试时，必须复用原 `requestId`。后端会返回已保存结果，不会重复增加判定次数。

## 4. 第二步：正式提交整改

```http
POST /river/openapi/v1/rectification/submit
```

权限：`river_rectification_complete`。

请求：

```json
{
  "rectificationId": "1930000000000000601",
  "version": 1,
  "requestId": "01980f75-6254-7b5a-a3d3-f62a0ec42002",
  "judgmentId": "1930000000000000701",
  "description": "现场垃圾已清理并完成复查"
}
```

| 字段              | 类型   | 必填 | 说明                                 |
| ----------------- | ------ | ---: | ------------------------------------ |
| `rectificationId` | string |   是 | 整改任务 ID                          |
| `version`         | number |   是 | AI 判定响应中的新版本                |
| `requestId`       | string |   是 | 正式提交的幂等号；不能复用 AI 请求号 |
| `judgmentId`      | string |   是 | 最近一次 AI 判定响应中的记录 ID      |
| `description`     | string |   是 | 整改说明，最长 1000 字符             |

成功响应 `data`：

```json
{
  "rectificationId": "1930000000000000601",
  "status": "COMPLETED",
  "aiResult": "COMPLETED",
  "aiJudgmentCount": 1,
  "version": 2,
  "forceRequired": false,
  "forceCompleted": false,
  "conclusion": "现场问题已整改完成"
}
```

正式提交不再调用 AI。后端从 `judgmentId` 对应的判定记录取得整改图片，在同一个事务中保存整改说明、完成整改任务、完成关联问题并写入操作日志。

如果 AI 判定后用户增加、删除、替换或重新上传图片，前端必须立即清空 `judgmentId`、设置 `canSubmit=false`，并要求重新执行第一步。即使前端漏做，后端也会拒绝非最近一次判定。

## 5. 连续三次未通过后的强制完成

```http
POST /river/openapi/v1/rectification/force-complete
```

权限：`river_rectification_force_complete`。

只有任务仍为 `PENDING`、累计判定次数不少于 3，且最近结果为 `INCOMPLETE` 或 `FAILED` 时才允许调用。

```json
{
  "rectificationId": "1930000000000000601",
  "version": 3,
  "requestId": "01980f75-6254-7b5a-a3d3-f62a0ec42003",
  "judgmentId": "1930000000000000703",
  "description": "现场垃圾已清理并完成复查",
  "reason": "现场网络异常，已由两名工作人员复核整改完成"
}
```

`judgmentId` 必须是最近一次未通过的判定记录；`description` 最长 1000 字符，`reason` 最长 500 字符。成功响应中 `status=COMPLETED`、`forceCompleted=true`。

## 6. 任务详情新增字段

以下接口均返回新增字段：

```http
GET /river/openapi/v1/task/page
GET /river/openapi/v1/task/detail?id={rectificationId}
```

| 字段               | 类型    | 说明                     |
| ------------------ | ------- | ------------------------ |
| `lastAiJudgmentId` | string  | 最近一次 AI 判定记录 ID  |
| `lastAiConclusion` | string  | 最近一次 AI 判定结论     |
| `canSubmit`        | boolean | 当前是否可以正常提交整改 |
| `forceRequired`    | boolean | 当前是否满足强制完成条件 |

页面重新进入或 AI 请求超时后，应先刷新任务详情恢复页面状态。若详情中的 `lastAiJudgmentId` 与页面保存值不同，以详情为准。

## 7. 推荐的小程序调用流程

```text
获取任务详情和 version
  → 上传整改图片，取得 imageRefs
  → 生成 aiRequestId
  → POST /rectification/ai-judgment
  → 保存 judgmentId、version、aiResult、canSubmit
  → AI通过后用户确认
  → 生成 submitRequestId
  → POST /rectification/submit
  → status=COMPLETED 后返回列表并刷新
```

示例：

```js
async function judgeRectification(task, imageRefs) {
  const result = await request({
    url: "/river/openapi/v1/rectification/ai-judgment",
    method: "POST",
    data: {
      rectificationId: String(task.id),
      version: task.version,
      requestId: createRequestId(),
      imageRefs,
    },
  });
  return result.data;
}

async function submitRectification(task, judgment, description) {
  const result = await request({
    url: "/river/openapi/v1/rectification/submit",
    method: "POST",
    data: {
      rectificationId: String(task.id),
      version: judgment.version,
      requestId: createRequestId(),
      judgmentId: String(judgment.judgmentId),
      description,
    },
  });
  return result.data;
}
```

写操作按钮点击后应立即进入 loading 并禁止重复点击。`requestId` 应在发起请求前生成并随待重试请求一起保存，不要在每次网络重试时重新生成。

## 8. 常见错误处理

| 后端提示                                 | 前端处理                         |
| ---------------------------------------- | -------------------------------- |
| `整改任务已被其他用户修改，请刷新后重试` | 重新获取任务详情及最新 `version` |
| `最近一次AI判定未通过，不能提交整改`     | 禁用普通提交，展示 AI 结论       |
| `整改图片已变化，请重新执行AI判定`       | 清空本地 `judgmentId`，重新判定  |
| `AI判定记录不存在`                       | 刷新任务详情，不继续提交         |
| `只有待整改任务可以处理`                 | 停止重复提交，刷新列表和详情     |
| `整改图片连续3次未通过后才能强制完成`    | 隐藏强制完成入口或刷新详情       |

## 9. 当前图片判定口径

接口允许上传多张整改图片并完整保存，但当前 AI 适配器只取第一张问题图片和第一张整改图片参与判定。前端应把最能体现整改结果的图片放在 `imageRefs[0]`；后续如 AI 服务支持多图，再统一调整该口径。
