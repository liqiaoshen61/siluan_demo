# 永春河湖监管小程序接口对接文档 V1

> 用途：供微信小程序前端开发、联调和验收使用。  
> 核对日期：2026-07-16  
> 接口版本：`v1`  
> 事实基线：当前后端工作树；不是 Git HEAD，也不是 2026-07-15 历史验收快照。

## 1. 文档范围与证据边界

### 1.1 当前接口范围

当前后端提供 28 个 `/river/openapi/v1/**` 移动端业务路由：

| 模块       |   数量 | 当前能力                                                         |
| ---------- | -----: | ---------------------------------------------------------------- |
| 当前用户   |      1 | 当前用户、角色和数据范围                                         |
| 基础选项   |      4 | 乡镇、河段、问题类型、可执行计划项                               |
| 问题上报   |      9 | 分页、详情、草稿、识别、提交、撤回、恢复、删除草稿、直接删除任务 |
| 整改任务   |      3 | 分页、详情、提交整改并执行 AI 判定                               |
| 通知       |      5 | 分页、未读数、单条已读、全部已读、删除                           |
| 移动端统计 |      6 | 总览、乡镇汇总、趋势、问题分布、逾期明细、计划进度               |
| **合计**   | **28** | 不含 BladeX 登录和通用 OSS 上传接口                              |

通用图片上传复用：

```text
POST /blade-resource/oss/endpoint/put-file
```

BladeX 登录和通用图片上传不属于 `/river/openapi/v1/**`，因此不计入上述 28 个路由。

### 1.2 资料与源码的使用规则

本文按以下优先级确定接口事实：

1. 当前工作树中的 Controller 路由、HTTP 方法和 `@PreAuth`；
2. 移动端 DTO、VO 及转换器；
3. Service、Mapper 中的状态、权限、事务和副作用；
4. 配置、数据库约束和通用 OSS 端点；
5. 原始访谈 DOCX、`其他说明.txt` 用于解释业务要求；
6. 小程序 HTML 原型只用于理解页面交互，不作为接口已实现的证据。

项目内目前**没有可编译、可发布的微信小程序工程**。`项目资料/03原型/小程序/index.html` 是使用 `localStorage` 和随机模拟数据的单页 HTML 原型，不是微信开发者工具工程。

### 1.3 业务资料口径与接口能力不是同一概念

项目资料确定了 4 个巡查组、初始总计 9 人、22 个乡镇、经纬度选填、背景图与问题图分开、问题图 AI 建议后人工确认、整改图 AI 判定及连续 3 次未通过后人工强制、每月 5 日前形成上月汇总等业务口径。

本文只描述当前移动端实际可调用的接口。某项要求出现在 DOCX、TXT、数据库或后台 Service 中，不代表小程序 OpenAPI 已经提供对应路由或返回字段。

---

## 2. 联调基础约定

### 2.1 Base URL 与合法域名

后端未配置 `context-path`，默认端口为 `8905`。

| 环境      | Base URL                    |
| --------- | --------------------------- |
| 本地直连  | `http://{host}:8905`        |
| 测试/生产 | 由部署人员提供的 HTTPS 地址 |

下文路径均相对于 Base URL，例如：

```text
GET {baseUrl}/river/openapi/v1/profile
```

部署环境可能通过 Nginx 或网关增加前缀，前端不得把本地端口或固定域名散落在页面代码中。

微信公众平台至少需要配置：

- `request` 合法域名：业务接口；
- `uploadFile` 合法域名：OSS 上传；
- `downloadFile` 合法域名：需要下载或临时访问文件时；
- 生产环境使用 HTTPS；
- API 域名与 OSS 文件域名可能不同，应分别确认。

### 2.2 登录、刷新与注销

移动端复用 BladeX OAuth2/JWT 登录体系。账号密码登录接口：

```http
POST /blade-auth/oauth/token
Authorization: Basic {Base64(clientId:clientSecret)}
Tenant-Id: {tenantId}
Blade-Requested-With: BladeHttpRequest
Content-Type: application/x-www-form-urlencoded
```

请求参数：

| 参数         | 类型   | 必填 | 说明                                                                            |
| ------------ | ------ | ---: | ------------------------------------------------------------------------------- |
| `tenantId`   | string |   是 | 租户编号，当前河湖默认租户为 `000000`                                           |
| `username`   | string |   是 | BladeX 登录账号                                                                 |
| `password`   | string |   是 | 使用部署环境 OAuth2 SM2 公钥加密后的密码，不传明文                              |
| `grant_type` | string |   是 | 无验证码环境使用 `password`；开启验证码时按部署配置使用 `captcha` 或 `behavior` |
| `scope`      | string |   是 | 当前使用 `all`                                                                  |

成功响应是 OAuth2 Token 原始结构，不使用 `R<T>` 包装：

```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "token_type": "bearer",
  "expires_in": 3599,
  "tenant_id": "000000",
  "user_id": "1930000000000000001"
}
```

刷新 Token 仍调用同一路径：

```http
POST /blade-auth/oauth/token?tenantId=000000&grant_type=refresh_token&scope=all&refresh_token={refreshToken}
```

注销当前登录：

```http
GET /blade-auth/oauth/logout
Blade-Auth: bearer {accessToken}
```

`clientId/clientSecret`、SM2 公钥、验证码模式由部署方提供。小程序不能复用后台生产客户端密钥，也不能内置微信 AppSecret；正式环境应创建权限最小化的小程序 OAuth Client。当前仍没有微信 `wx.login` 的 `code` 换取 BladeX Token、微信账号绑定接口，现阶段只能使用已绑定的 BladeX 账号登录。

### 2.3 鉴权与请求头

业务接口需要有效 BladeX 登录令牌，并受菜单权限和服务端数据范围控制。

JSON 请求建议使用：

```http
Blade-Auth: bearer {accessToken}
Blade-Requested-With: BladeHttpRequest
Accept-Language: zh-CN
Content-Type: application/json
```

GET 请求通常不需要 `Content-Type`，但仍需前两个鉴权相关请求头。

登录成功后保存 `access_token` 和 `refresh_token`。业务请求收到 `401` 时只允许发起一次刷新；刷新失败则清理本地令牌并回到登录页。

### 2.4 统一响应 `R<T>`

除文件上传返回的业务类型不同外，JSON 接口统一使用 BladeX `R<T>` 包装：

```json
{
  "code": 200,
  "success": true,
  "data": {},
  "msg": "操作成功"
}
```

| 字段      | 前端类型 | 说明                                  |
| --------- | -------- | ------------------------------------- |
| `code`    | number   | BladeX 业务状态码，`200` 通常表示成功 |
| `success` | boolean  | 是否成功                              |
| `data`    | any      | 业务数据；命令类接口可能为空          |
| `msg`     | string   | 成功提示或业务错误原因                |

前端处理顺序：

1. 处理网络超时、断网和 HTTP 状态；
2. 判断响应体 `code`/`success`；
3. 失败时优先展示 `msg`；
4. 收到 `401` 时进入 Token 刷新或重新登录流程；
5. 不要只依据 HTTP 200 判断业务成功。

业务异常主要由后端 `ServiceException` 返回中文 `msg`。精确 HTTP 状态和错误码映射由 BladeX 全局异常处理决定，需以实际运行环境为准。

### 2.5 分页 `IPage<T>`

分页请求通用参数：

| 参数      | 类型   |   必填 | 说明                                 |
| --------- | ------ | -----: | ------------------------------------ |
| `current` | number | 建议是 | 当前页，从 `1` 开始                  |
| `size`    | number | 建议是 | 每页条数，例如 `10`                  |
| `ascs`    | string |     否 | 升序字段；小程序不应透传任意用户输入 |
| `descs`   | string |     否 | 降序字段；小程序不应透传任意用户输入 |

分页响应：

```json
{
  "code": 200,
  "success": true,
  "data": {
    "records": [],
    "total": 0,
    "size": 10,
    "current": 1,
    "pages": 0
  },
  "msg": "操作成功"
}
```

### 2.6 ID、时间和空值

#### Long ID

后端开启大数字转字符串。所有用户、乡镇、问题、任务、通知等 Long ID 均应在小程序中按 `string` 保存和传递，禁止转为 JavaScript `Number`。

```js
const inspectionId = "1930000000000000501";
```

#### 时间

时间格式按使用场景区分：

| 场景                      | 当前约定                                                           |
| ------------------------- | ------------------------------------------------------------------ |
| GET 查询时间参数          | `yyyy-MM-dd HH:mm:ss`，需要 URL 编码空格                           |
| 移动端 VO 响应时间        | `yyyy-MM-dd HH:mm:ss`                                              |
| JSON Body 的 `capturedAt` | Java `LocalDateTime`，DTO 未声明局部格式；正式联调时以运行环境确认 |

`capturedAt` 是客户端提交的采集辅助时间。正式问题业务时间 `inspectionTime`、提交时间 `submittedAt` 以及整改完成时间均由服务器生成，客户端不能指定或覆盖。

当前移动端 `OpenInspectionVO` 不返回 `capturedAt`，只返回服务器正式时间 `inspectionTime`。前端不能依赖详情接口恢复该采集辅助时间。

后端启用了 null 转空值配置，部分空字段在运行环境中可能表现为空字符串、空数组或空对象。前端应做空值兼容，不要只判断 `null`。

### 2.7 权限矩阵

| 接口能力                                                                                | 当前服务端权限                                                    |
| --------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 当前用户、基础选项、问题分页/详情、保存草稿、图片识别、恢复草稿、删除草稿、直接删除任务 | `river_inspection`                                                |
| 提交问题                                                                                | 有效登录 + `river_inspection_submit`；方法级注解覆盖类级权限      |
| 撤回问题                                                                                | 有效登录 + `river_inspection_withdraw`；方法级注解覆盖类级权限    |
| 整改任务分页、详情                                                                      | `river_rectification`                                             |
| 提交整改并执行 AI                                                                       | 有效登录 + `river_rectification_complete`；方法级注解覆盖类级权限 |
| 通知分页、未读数、已读、全部已读、删除                                                  | 当前均为类级 `river_notification`                                 |
| 移动端统计 6 个查询接口                                                                 | `river_statistics`                                                |

移动端通知写操作当前没有管理端对应的 `river_notification_read`、`river_notification_delete` 方法级限制。本文按实际实现记录，不表示该差异是推荐权限设计。

BladeX 当前对同一方法上的 `@PreAuth` 采用“方法级优先、否则类级”，并非自动合并。因此提交、撤回和整改完成实际只检查各自方法级按钮权限（同时仍要求有效登录）；若产品要求父菜单权限也必须具备，应由后端调整权限表达式或校验方式。

### 2.8 数据范围

所有数据范围由 Token 中的租户、用户、角色、部门和巡查组关系在服务端计算，前端传参不能扩大范围。

| 角色/场景  | 主要范围                         |
| ---------- | -------------------------------- |
| 县级管理员 | 当前租户全县                     |
| 乡镇用户   | 当前部门绑定的乡镇               |
| 巡查员     | 当前用户所属的启用巡查组         |
| 通知       | 当前登录用户本人作为收件人的通知 |

问题分页接口虽然后端摘要为“我的巡查问题分页”，实际语义是“当前用户数据权限范围内的问题”，不一定严格等于 `reporterId` 为本人。

草稿编辑、提交、撤回、恢复和删除还要求操作者是原上报人；县级全县权限用户除外。整改提交要求操作者有任务责任乡镇的数据权限。

---

## 3. 状态、幂等与乐观锁

### 3.1 问题状态机

```text
DRAFT（草稿）
  ├─ 提交 → RECTIFYING（整改中），创建整改任务
  ├─ 删除草稿 → 逻辑删除
  └─ 直接删除任务 → 逻辑删除

RECTIFYING（整改中）
  ├─ 撤回 → WITHDRAWN（已撤回），取消待整改任务
  ├─ 整改 AI 判定 COMPLETED → COMPLETED（已完成）
  └─ 直接删除任务 → 逻辑删除并删除关联整改任务

WITHDRAWN（已撤回）
  ├─ 恢复 → DRAFT（草稿）
  └─ 直接删除任务 → 逻辑删除

COMPLETED（已完成）
  └─ 直接删除任务 → 逻辑删除并删除关联整改任务
```

业务限制：

- 只有 `DRAFT` 可以编辑、提交或使用“删除草稿”；
- “直接删除任务”支持所有问题状态，但只允许原上报人或县级管理员；会校验乐观锁，逻辑删除问题和关联整改任务、清理参与人员快照，并重新计算计划项状态；
- 只有 `RECTIFYING` 可以撤回；
- 当前撤回校验只把状态已为 `COMPLETED` 的整改任务视为“已提交结果”；AI 返回 `INCOMPLETE` 或 `FAILED` 后虽然说明、图片和判断记录已经保存，问题仍可撤回；
- 只有 `WITHDRAWN` 可以恢复为草稿；
- 整改 AI 返回 `INCOMPLETE` 或 `FAILED` 时，问题仍为 `RECTIFYING`，任务仍为 `PENDING`；
- 撤回会取消待整改任务，并可能恢复计划项为待执行；
- 恢复后重新提交会建立新的整改任务；但当前整改表唯一键包含 `(tenantId, inspectionId, isDeleted)`，历史已删除任务仍保留，因此同一问题完成一次“撤回 → 恢复 → 重提”后，第二次撤回可能因唯一键冲突而失败。前端不得假设该循环可无限重复，需在联调中验证并由后端修复数据模型。

### 3.2 主要枚举

#### 问题状态 `InspectionStatus`

| 值           | 中文   |
| ------------ | ------ |
| `DRAFT`      | 草稿   |
| `RECTIFYING` | 整改中 |
| `COMPLETED`  | 已完成 |
| `WITHDRAWN`  | 已撤回 |

#### 整改状态 `RectificationStatus`

| 值          | 中文   |
| ----------- | ------ |
| `PENDING`   | 待整改 |
| `COMPLETED` | 已完成 |

#### 整改 AI 结果 `RectificationAiResult`

| 值           | 说明                        |
| ------------ | --------------------------- |
| `COMPLETED`  | AI 判定整改完成             |
| `INCOMPLETE` | AI 判定未完成               |
| `FAILED`     | AI 调用异常或未返回有效结果 |

查询参数中的 Java 枚举值区分大小写，应传英文枚举名。

### 3.3 幂等请求号 `requestId`

提交、撤回、恢复、删除和整改提交等状态操作需要 `requestId`。

- 推荐 UUID/ULID，客户端主动限制为不超过 64 字符；
- 当前 DTO 只校验非空，未统一声明 64 字符上限；超过 64 字符时，不同业务分支可能表现为数据库错误或未落幂等键，不能依赖服务端给出稳定的参数校验提示；
- 问题提交、撤回、恢复、删除会在动作表中按 `tenantId + requestId + actionType` 判重；
- 上述问题动作因超时重试时复用原 `requestId`；
- 不同业务对象、不同新操作应生成新 `requestId`；
- 不同问题不得复用同一动作类型的 `requestId`，否则可能被误判为已处理。

> **整改幂等边界：**整改提交不是完整的幂等闭环。当前会先校验任务仍为 `PENDING`，再从完成动作表检查 `requestId`；如果第一次已完成，同一请求重放可能先收到“只有待整改任务可以处理”。如果第一次返回 `INCOMPLETE` 或 `FAILED`，后端不会持久化该 `requestId`：使用原 `version` 重放会再次调用 AI 后产生版本冲突；刷新为新 `version` 再复用原 `requestId` 会被当作新一次判断并增加次数。因此整改请求超时后不得自动盲目重放，应先刷新任务并由用户确认；当前任务详情又不返回最近 AI 结论，无法完全恢复丢失的首次响应。

### 3.4 乐观锁 `version`

- 编辑草稿和所有状态操作均应携带详情中的最新 `version`；
- 版本冲突时重新获取详情，再由用户确认是否重试；
- 保存草稿只返回草稿 ID，不返回更新后的版本；保存成功后应重新获取详情；
- 整改 AI 未通过或调用失败仍会记录结果并递增 `version`；前端必须采用返回结果中的新 `version`；
- 撤回、恢复、删除等操作后，如还需继续处理，应刷新列表或详情。

---

## 4. 图片上传

业务接口只接收上传后的图片引用，不接收 Base64 或二进制正文。

### 4.1 通用 OSS 上传

```http
POST /blade-resource/oss/endpoint/put-file
Content-Type: multipart/form-data
```

请求参数：

| 参数   | 位置           | 类型   | 必填 | 说明                                                                                 |
| ------ | -------------- | ------ | ---: | ------------------------------------------------------------------------------------ |
| `file` | multipart 表单 | file   |   是 | 待上传文件                                                                           |
| `code` | URL Query      | string |   否 | 当前租户的 OSS 配置编码；传入后优先选择对应配置，未传或未找到时回退环境默认 OSS 配置 |

需要指定存储配置时使用：

```http
POST /blade-resource/oss/endpoint/put-file?code={ossCode}
```

响应示例：

```json
{
  "code": 200,
  "success": true,
  "data": {
    "link": "https://example.com/tenant-bucket/upload/2026-07-16/uuid.jpg",
    "domain": "https://example.com/tenant-bucket",
    "name": "upload/2026-07-16/uuid.jpg",
    "originalName": "example.jpg",
    "attachId": ""
  },
  "msg": "操作成功"
}
```

业务请求优先保存 `data.link`。如果部署环境不返回 `link`，应由后端或部署方确认是否保存 `data.name`，前端不得自行拼接域名。

微信上传示意：

```js
wx.uploadFile({
  url: `${baseUrl}/blade-resource/oss/endpoint/put-file`,
  filePath,
  name: "file",
  header: {
    "Blade-Auth": `bearer ${accessToken}`,
    "Blade-Requested-With": "BladeHttpRequest",
    "Accept-Language": "zh-CN",
  },
});
```

### 4.2 当前限制与边界

| 业务字段              | 当前后端校验                                          |
| --------------------- | ----------------------------------------------------- |
| `backgroundImageRefs` | 最多 9 个；每个引用非空且最长 512 字符                |
| `problemImageRefs`    | 最多 9 个；每个引用非空且最长 512 字符                |
| 整改 `imageRefs`      | 数组非空、元素非空；当前 DTO 未设置 9 张/512 字符限制 |

当前没有 `/river/openapi/v1/**` 下的专用上传接口，也未在该 OSS Controller 中看到河湖业务专用的 MIME、扩展名、文件大小或图片尺寸校验。正式环境需确认：

- 文件格式、单文件大小、图片数量和压缩策略；
- OSS 访问权限、签名 URL、生命周期和删除策略；
- 上传后草稿保存失败、用户取消或删除引用时的孤儿文件清理；
- 小程序上传和图片访问合法域名。

---

## 5. 接口速查

BladeX 通用鉴权接口不计入下方 28 个业务路由：

| 能力         | 方法 | 路径                                                              |
| ------------ | ---- | ----------------------------------------------------------------- |
| 账号密码登录 | POST | `/blade-auth/oauth/token`，`grant_type=password/captcha/behavior` |
| 刷新 Token   | POST | `/blade-auth/oauth/token`，`grant_type=refresh_token`             |
| 注销         | GET  | `/blade-auth/oauth/logout`                                        |

| 序号 | 模块 | 方法   | 路径                                                | 说明                         |
| ---: | ---- | ------ | --------------------------------------------------- | ---------------------------- |
|    1 | 用户 | GET    | `/river/openapi/v1/profile`                         | 当前用户资料                 |
|    2 | 选项 | GET    | `/river/openapi/v1/options/townships`               | 可选乡镇                     |
|    3 | 选项 | GET    | `/river/openapi/v1/options/sections`                | 可选河段                     |
|    4 | 选项 | GET    | `/river/openapi/v1/options/problem-types`           | 问题类型选项                 |
|    5 | 选项 | GET    | `/river/openapi/v1/options/plan-items`              | 可执行计划项                 |
|    6 | 问题 | GET    | `/river/openapi/v1/inspection/page`                 | 问题分页                     |
|    7 | 问题 | GET    | `/river/openapi/v1/inspection/detail`               | 问题详情                     |
|    8 | 问题 | POST   | `/river/openapi/v1/inspection/draft`                | 保存问题草稿                 |
|    9 | 问题 | POST   | `/river/openapi/v1/inspection/recognition`          | 识别问题图片                 |
|   10 | 问题 | POST   | `/river/openapi/v1/inspection/submit`               | 提交问题                     |
|   11 | 问题 | POST   | `/river/openapi/v1/inspection/withdraw`             | 撤回问题                     |
|   12 | 问题 | POST   | `/river/openapi/v1/inspection/draft/restore`        | 恢复为草稿                   |
|   13 | 问题 | DELETE | `/river/openapi/v1/inspection/draft`                | 删除草稿，使用 JSON Body     |
|   14 | 问题 | DELETE | `/river/openapi/v1/inspection/task`                 | 直接删除任务，使用 JSON Body |
|   15 | 整改 | GET    | `/river/openapi/v1/task/page`                       | 整改任务分页                 |
|   16 | 整改 | GET    | `/river/openapi/v1/task/detail`                     | 整改任务详情                 |
|   17 | 整改 | POST   | `/river/openapi/v1/rectification/complete`          | 提交整改并执行 AI 判定       |
|   18 | 通知 | GET    | `/river/openapi/v1/notification/page`               | 通知分页                     |
|   19 | 通知 | GET    | `/river/openapi/v1/notification/unread-count`       | 未读数                       |
|   20 | 通知 | POST   | `/river/openapi/v1/notification/read`               | 标记单条已读                 |
|   21 | 通知 | POST   | `/river/openapi/v1/notification/read-all`           | 全部标记已读                 |
|   22 | 通知 | DELETE | `/river/openapi/v1/notification/remove`             | 删除本人通知                 |
|   23 | 统计 | GET    | `/river/openapi/v1/statistics/overview`             | 统计总览                     |
|   24 | 统计 | GET    | `/river/openapi/v1/statistics/township-summary`     | 乡镇汇总                     |
|   25 | 统计 | GET    | `/river/openapi/v1/statistics/trend`                | 月度趋势                     |
|   26 | 统计 | GET    | `/river/openapi/v1/statistics/problem-distribution` | 问题类型分布                 |
|   27 | 统计 | GET    | `/river/openapi/v1/statistics/overdue-cases`        | 逾期问题明细                 |
|   28 | 统计 | GET    | `/river/openapi/v1/statistics/plan-progress`        | 月度计划进度                 |

---

## 6. 当前用户

### 6.1 获取当前用户资料

```http
GET /river/openapi/v1/profile
```

权限：`river_inspection`。

响应 `data`：

| 字段          | 类型     | 说明                     |
| ------------- | -------- | ------------------------ |
| `id`          | string   | 当前 BladeX 用户 ID      |
| `account`     | string   | 登录账号                 |
| `name`        | string   | 用户名                   |
| `realName`    | string   | 真实姓名                 |
| `avatar`      | string   | 头像                     |
| `phone`       | string   | 手机号，前端注意隐私展示 |
| `roleAliases` | string[] | 当前角色别名集合         |
| `countyWide`  | boolean  | 是否拥有全县范围         |
| `townshipIds` | string[] | 可访问乡镇 ID 集合       |
| `groupIds`    | string[] | 可访问巡查组 ID 集合     |

响应示例：

```json
{
  "code": 200,
  "success": true,
  "data": {
    "id": "1930000000000000001",
    "account": "inspector01",
    "name": "巡查员01",
    "realName": "巡查员甲",
    "avatar": "",
    "phone": "13800000000",
    "roleAliases": ["river_inspector"],
    "countyWide": false,
    "townshipIds": [],
    "groupIds": ["1930000000000000901"]
  },
  "msg": "操作成功"
}
```

建议在应用启动并取得 Token 后首先调用本接口，用于展示用户信息和辅助控制页面入口。最终权限仍以服务端返回为准，不能只依赖前端隐藏按钮。

---

## 7. 基础选项

以下 4 个接口权限均为 `river_inspection`。

### 7.1 当前用户可选乡镇

```http
GET /river/openapi/v1/options/townships
```

响应 `data` 为数组：

```json
[
  {
    "id": "1930000000000000201",
    "name": "桃城镇",
    "code": "TAOCHENG"
  }
]
```

字段：`id`、`name`、`code`。结果按当前用户的 `townshipIds` 范围过滤。纯巡查员通常只有巡查组范围、没有直接乡镇范围，因此该接口当前可能返回空数组；巡查组范围不会自动映射成乡镇选项。

### 7.2 当前用户可选河段

```http
GET /river/openapi/v1/options/sections?townshipId={townshipId}
```

| 参数         | 类型   | 必填 | 说明                                     |
| ------------ | ------ | ---: | ---------------------------------------- |
| `townshipId` | string |   否 | 按乡镇过滤；传入值仍受服务端数据范围控制 |

响应字段同乡镇选项：`id`、`name`、`code`。该接口同样只识别当前用户的乡镇范围，不识别巡查组范围；纯巡查员即使能从计划项看到所属乡镇，当前也可能无法取得该乡镇的河段选项。

```json
[
  {
    "id": "1930000000000000301",
    "name": "桃溪桃城段",
    "code": "SECTION_001"
  }
]
```

### 7.3 问题类型选项

```http
GET /river/openapi/v1/options/problem-types
```

响应 `data`：

```json
[
  {
    "id": "1930000000000000401",
    "parentId": "0",
    "name": "乱堆",
    "code": "DUMPING",
    "level": 1
  },
  {
    "id": "1930000000000000402",
    "parentId": "1930000000000000401",
    "name": "生活垃圾",
    "code": "DOMESTIC_WASTE",
    "level": 2
  }
]
```

该接口返回带 `parentId`、`level` 的**扁平列表**，不是嵌套 `children` 树。前端需要自行构造级联选择树，并确保子类的 `parentId` 等于所选大类 ID。

问题类型最终业务字典仍需业务方确认；前端应使用接口返回的 ID，不要硬编码原型中的简化分类。

> **当前实现差异：**选项查询只过滤租户和逻辑删除，未过滤 `status=1`；停用但未删除的类型仍可能返回。保存草稿时却只接受启用类型，因此前端选中停用项会收到“问题大类无效”或父子关系错误。联调前应由后端统一选项和保存校验口径。

### 7.4 当前用户可执行计划项

```http
GET /river/openapi/v1/options/plan-items
```

响应 `data`：

```json
[
  {
    "planItemId": "1930000000000000101",
    "planMonth": "2026-07",
    "townshipId": "1930000000000000201",
    "townshipName": "桃城镇",
    "groupId": "1930000000000000901",
    "groupName": "第一巡查组"
  }
]
```

只返回当前用户数据范围内、查询时处于 `PENDING` 的已发布计划明细。选择计划项后，草稿中的 `townshipId` 必须与该计划项一致。

> **计划项一致性缺口：**选项列表会过滤 `executionStatus=PENDING`，但保存草稿按客户端 `planItemId` 直接查询时只校验计划已发布，不复核明细仍为 `PENDING`；提交时也不再次检查。因此直接传入已完成计划项，或同一待执行计划项预存多个草稿后依次提交，均可能重复生成问题和整改任务。前端只能使用本次最新选项结果，提交前应刷新选项，但这不能代替后端修复状态校验和唯一约束。

> **停用乡镇缺口：**计划项选项和草稿校验未检查关联乡镇 `status=1`。已发布计划中的乡镇被停用后，巡查员仍可能取得计划项并提交问题，系统会创建责任乡镇为该停用乡镇的整改任务；普通乡镇用户的数据范围却已排除该乡镇，无法处理任务，只能依赖县级用户兜底。

> **参与人员缺口：**当前 28 个 OpenAPI 中没有“可选参与人员”接口，但保存草稿要求至少 2 名有效 `participantUserIds`。正式联调前必须明确参与人员 ID 的安全来源或补充移动端选项接口，不能使用原型虚拟人员或前端硬编码用户 ID。

---

## 8. 问题上报

接口前缀：

```text
/river/openapi/v1/inspection
```

### 8.1 问题分页

```http
GET /river/openapi/v1/inspection/page
```

权限：`river_inspection`。

查询参数：

| 参数                 | 类型    |   必填 | 说明                                                                           |
| -------------------- | ------- | -----: | ------------------------------------------------------------------------------ |
| `current`            | number  | 建议是 | 当前页                                                                         |
| `size`               | number  | 建议是 | 每页条数                                                                       |
| `status`             | string  |     否 | `DRAFT`、`RECTIFYING`、`COMPLETED`、`WITHDRAWN`                                |
| `townshipId`         | string  |     否 | 乡镇 ID                                                                        |
| `sectionId`          | string  |     否 | 河段 ID                                                                        |
| `issuedStartTime`    | string  |     否 | 按问题 `submittedAt` 过滤的开始时间，`yyyy-MM-dd HH:mm:ss`                     |
| `issuedEndTime`      | string  |     否 | 按问题 `submittedAt` 过滤的结束时间                                            |
| `completedStartTime` | string  |     否 | **当前实际按整改截止时间 `deadlineAt` 过滤**的开始时间；参数名与实现语义不一致 |
| `completedEndTime`   | string  |     否 | **当前实际按整改截止时间 `deadlineAt` 过滤**的结束时间；不是整改完成时间       |
| `overdueOnly`        | boolean |     否 | `true` 只查逾期问题                                                            |

请求示例：

```http
GET /river/openapi/v1/inspection/page?current=1&size=10&status=RECTIFYING&townshipId=1930000000000000201
```

响应中的每条 `OpenInspectionVO`：

| 字段                                      | 类型     | 说明                                                                                        |
| ----------------------------------------- | -------- | ------------------------------------------------------------------------------------------- |
| `id`                                      | string   | 问题 ID                                                                                     |
| `planItemId`                              | string   | 计划明细 ID                                                                                 |
| `townshipId` / `townshipName`             | string   | 乡镇                                                                                        |
| `sectionId` / `sectionName`               | string   | 河段                                                                                        |
| `groupId` / `groupName`                   | string   | 巡查组                                                                                      |
| `reporterId` / `reporterName`             | string   | 上报人                                                                                      |
| `inspectionTime`                          | string   | 服务端正式暗访时间，`yyyy-MM-dd HH:mm:ss`                                                   |
| `longitude` / `latitude`                  | number   | 坐标，可能为空                                                                              |
| `location`                                | string   | 问题地点                                                                                    |
| `problemTypeId` / `problemTypeName`       | string   | 问题大类                                                                                    |
| `problemSubtypeId` / `problemSubtypeName` | string   | 问题子类                                                                                    |
| `description`                             | string   | 问题描述                                                                                    |
| `backgroundImages`                        | string[] | 背景图引用数组                                                                              |
| `problemImages`                           | string[] | 问题图引用数组                                                                              |
| `participants`                            | object[] | 参与人员快照，字段为 `userId`、`userName`；当前分页查询未装载该字段，通常仅详情返回完整数据 |
| `inspectionStatus`                        | string   | 问题状态                                                                                    |
| `submittedAt`                             | string   | 提交时间                                                                                    |
| `rectificationDeadlineAt`                 | string   | 整改截止时间                                                                                |
| `rectificationStatus`                     | string   | 整改状态                                                                                    |
| `version`                                 | number   | 乐观锁版本                                                                                  |
| `overdue`                                 | boolean  | 是否逾期                                                                                    |

分页响应示例：

```json
{
  "code": 200,
  "success": true,
  "data": {
    "records": [
      {
        "id": "1930000000000000501",
        "planItemId": "1930000000000000101",
        "townshipId": "1930000000000000201",
        "townshipName": "桃城镇",
        "sectionId": "1930000000000000301",
        "sectionName": "桃溪桃城段",
        "groupId": "1930000000000000901",
        "groupName": "第一巡查组",
        "reporterId": "1930000000000000001",
        "reporterName": "巡查员甲",
        "inspectionTime": "2026-07-16 14:30:00",
        "longitude": 118.2945211,
        "latitude": 25.3214567,
        "location": "桃城镇某河段桥下",
        "problemTypeId": "1930000000000000401",
        "problemTypeName": "乱堆",
        "problemSubtypeId": "1930000000000000402",
        "problemSubtypeName": "生活垃圾",
        "description": "河道边发现生活垃圾堆放",
        "backgroundImages": ["https://example.com/river/background-1.jpg"],
        "problemImages": ["https://example.com/river/problem-1.jpg"],
        "participants": [
          { "userId": "1930000000000000001", "userName": "巡查员甲" },
          { "userId": "1930000000000000002", "userName": "巡查员乙" }
        ],
        "inspectionStatus": "RECTIFYING",
        "submittedAt": "2026-07-16 14:31:00",
        "rectificationDeadlineAt": "2026-07-23 14:31:00",
        "rectificationStatus": "PENDING",
        "version": 1,
        "overdue": false
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  },
  "msg": "操作成功"
}
```

列表按当前县级、乡镇或巡查组数据范围过滤，并非只按上报人过滤。

当前分页主查询未过滤问题通用 `status=1`，而非县级用户请求详情时的数据范围查询会要求 `status=1`。因此停用但未删除的问题可能出现在非县级用户列表中，随后请求详情却被拒绝；前端需兼容并由后端统一列表/详情谓词。

### 8.2 问题详情

```http
GET /river/openapi/v1/inspection/detail?id={inspectionId}
```

权限：`river_inspection`。

| 参数 | 类型   | 必填 | 说明                   |
| ---- | ------ | ---: | ---------------------- |
| `id` | string |   是 | 问题 ID，放在 Query 中 |

成功响应 `data` 与分页记录的 `OpenInspectionVO` 一致。详情不存在时，当前 Service 可能返回空 `data`；前端应同时处理空数据和错误响应。

### 8.3 保存问题草稿

```http
POST /river/openapi/v1/inspection/draft
Content-Type: application/json
```

权限：`river_inspection`。

请求字段：

| 字段                  | 类型     |           必填 | 说明                                              |
| --------------------- | -------- | -------------: | ------------------------------------------------- |
| `id`                  | string   | 新建否，编辑是 | 草稿 ID                                           |
| `planItemId`          | string   |       条件必填 | 已发布计划明细 ID；纯巡查员通常必须传，见下方说明 |
| `townshipId`          | string   |             是 | 乡镇 ID                                           |
| `sectionId`           | string   |             否 | 河段 ID，必须属于乡镇                             |
| `capturedAt`          | string   |             否 | 客户端采集辅助时间；JSON 格式待运行环境确认       |
| `longitude`           | number   |             否 | 经度，`-180` 至 `180`                             |
| `latitude`            | number   |             否 | 纬度，`-90` 至 `90`                               |
| `location`            | string   |             否 | 最长 255 字符；提交前必填                         |
| `problemTypeId`       | string   |             否 | 顶级问题类型；提交前必填                          |
| `problemSubtypeId`    | string   |             否 | 子类，必须属于大类                                |
| `description`         | string   |             否 | 最长 1000 字符；提交前必填                        |
| `backgroundImageRefs` | string[] |             否 | 最多 9 个，每项最长 512；提交前至少 1 张          |
| `problemImageRefs`    | string[] |             否 | 最多 9 个，每项最长 512；提交前至少 1 张          |
| `participantUserIds`  | string[] |         **是** | 去重后至少 2 名有效用户，最多 50 名               |
| `version`             | number   | 新建否，编辑是 | 非负整数、最新版本                                |

> 接口名称虽为“保存草稿”，当前 Service 仍要求 `townshipId` 和至少 2 名有效参与人员。不能只发送一个空壳草稿。

`planItemId` 的实际必填性取决于数据范围：不传计划项时，Service 会要求当前用户直接拥有所选乡镇范围。纯 `river_inspector` 通常只有 `groupIds`、没有 `townshipIds`，因此应先从 `/options/plan-items` 选择本组计划项并传入 `planItemId`；县级或拥有对应乡镇范围的用户才可能不传。

经纬度规则：可同时为空；如果填写，必须两项同时存在并满足范围。

新建示例：

```json
{
  "planItemId": "1930000000000000101",
  "townshipId": "1930000000000000201",
  "sectionId": "1930000000000000301",
  "capturedAt": "2026-07-16T14:30:00",
  "longitude": 118.2945211,
  "latitude": 25.3214567,
  "location": "桃城镇某河段桥下",
  "problemTypeId": "1930000000000000401",
  "problemSubtypeId": "1930000000000000402",
  "description": "河道边发现生活垃圾堆放",
  "backgroundImageRefs": ["https://example.com/river/background-1.jpg"],
  "problemImageRefs": ["https://example.com/river/problem-1.jpg"],
  "participantUserIds": ["1930000000000000001", "1930000000000000002"]
}
```

`capturedAt` 示例仅表示建议联调值，最终 JSON Body 格式必须在运行环境确认。

编辑示例：

```json
{
  "id": "1930000000000000501",
  "townshipId": "1930000000000000201",
  "sectionId": "1930000000000000301",
  "location": "桃城镇某河段桥下",
  "problemTypeId": "1930000000000000401",
  "problemSubtypeId": "1930000000000000402",
  "description": "河道边发现生活垃圾堆放",
  "backgroundImageRefs": ["https://example.com/river/background-1.jpg"],
  "problemImageRefs": ["https://example.com/river/problem-1.jpg"],
  "participantUserIds": ["1930000000000000001", "1930000000000000002"],
  "version": 0
}
```

成功响应：

```json
{
  "code": 200,
  "success": true,
  "data": "1930000000000000501",
  "msg": "操作成功"
}
```

`data` 仅为草稿 ID。编辑保存后应调用详情接口获取最新 `version`。

### 8.4 识别问题图片

```http
POST /river/openapi/v1/inspection/recognition
Content-Type: application/json
```

权限：`river_inspection`。

| 字段           | 类型   | 必填 | 说明                                                                                                |
| -------------- | ------ | ---: | --------------------------------------------------------------------------------------------------- |
| `inspectionId` | string |   否 | 已保存问题 ID；传入时校验数据访问权限                                                               |
| `imageRef`     | string |   是 | 已上传的问题图片引用；客户端限制不超过 512 字符，当前 DTO 未做长度校验但识别记录列为 `VARCHAR(512)` |

请求：

```json
{
  "inspectionId": "1930000000000000501",
  "imageRef": "https://example.com/river/problem-1.jpg"
}
```

响应：

```json
{
  "code": 200,
  "success": true,
  "data": {
    "suggestedTypeId": "1930000000000000401",
    "suggestedSubtypeId": "1930000000000000402",
    "suggestedLocation": "桃城镇某河段桥下",
    "confidence": 0.92
  },
  "msg": "操作成功"
}
```

识别结果只是表单建议，用户必须能够人工确认和修改后再保存。接口存在不代表正式图片 AI 已完成真实供应商联调；开发环境可能使用 Mock。识别异常时后端可能返回“图片识别失败，请手工填写”。

### 8.5 提交问题

```http
POST /river/openapi/v1/inspection/submit
Content-Type: application/json
```

权限：有效登录 + `river_inspection_submit`。BladeX 当前按方法级 `@PreAuth` 优先解析，不会再叠加类级 `river_inspection`。

请求：

```json
{
  "inspectionId": "1930000000000000501",
  "version": 0,
  "requestId": "01980f74-61fa-7c92-b13e-4eb5bbd42001"
}
```

| 字段           | 类型   | 必填 | 说明       |
| -------------- | ------ | ---: | ---------- |
| `inspectionId` | string |   是 | 草稿 ID    |
| `version`      | number |   是 | 最新版本   |
| `requestId`    | string |   是 | 幂等请求号 |

提交前后端校验：

1. 状态为 `DRAFT`；
2. 操作者是上报人或县级全县权限用户；
3. 乡镇、位置、描述、问题大类不为空；
4. 背景图和问题图分别至少 1 张；
5. 至少有 2 条参与人员快照；保存草稿时会校验用户有效性，但提交时只重新统计快照数量，不会复验用户此刻是否仍启用；
6. 经纬度同时为空或同时合法；
7. 河段属于乡镇，子类属于大类；
8. `version` 为最新版本。

成功后：

- 问题 `DRAFT → RECTIFYING`；
- 服务器生成正式问题时间和提交时间；
- 创建乡镇整改任务；
- 关联计划项标记完成；
- 写入操作轨迹。

响应：

```json
{
  "code": 200,
  "success": true,
  "data": "",
  "msg": "提交成功"
}
```

### 8.6 撤回问题

```http
POST /river/openapi/v1/inspection/withdraw
Content-Type: application/json
```

权限：有效登录 + `river_inspection_withdraw`。BladeX 当前按方法级 `@PreAuth` 优先解析，不会再叠加类级 `river_inspection`。

请求：

```json
{
  "inspectionId": "1930000000000000501",
  "version": 1,
  "requestId": "01980f74-61fa-7c92-b13e-4eb5bbd42002",
  "remark": "现场信息需要补充"
}
```

| 字段           | 类型   | 必填 | 说明                                                                                         |
| -------------- | ------ | ---: | -------------------------------------------------------------------------------------------- |
| `inspectionId` | string |   是 | 问题 ID                                                                                      |
| `version`      | number |   是 | 最新版本                                                                                     |
| `requestId`    | string |   是 | 幂等请求号                                                                                   |
| `remark`       | string |   否 | 撤回原因/备注；客户端限制不超过 500 字符，当前 DTO 未做长度校验但操作轨迹列为 `VARCHAR(500)` |

限制：只有 `RECTIFYING` 可撤回；操作者必须是上报人或县级全县权限用户。当前后端只有在整改任务已经 `COMPLETED` 时才禁止撤回；AI 返回 `INCOMPLETE` 或 `FAILED` 后，即使整改说明、图片和判断记录已保存，仍允许撤回。

成功后问题变为 `WITHDRAWN`，待整改任务被取消，必要时恢复计划项待执行状态。

### 8.7 恢复已撤回问题为草稿

```http
POST /river/openapi/v1/inspection/draft/restore
Content-Type: application/json
```

权限：`river_inspection`。

请求：

```json
{
  "inspectionId": "1930000000000000501",
  "version": 2,
  "requestId": "01980f74-61fa-7c92-b13e-4eb5bbd42003"
}
```

只有 `WITHDRAWN` 可恢复，成功后状态为 `DRAFT`。恢复后应刷新详情，再编辑保存并使用最新版本重新提交。

### 8.8 删除草稿

```http
DELETE /river/openapi/v1/inspection/draft
Content-Type: application/json
```

权限：`river_inspection`。

请求 Body：

```json
{
  "inspectionId": "1930000000000000501",
  "version": 0,
  "requestId": "01980f74-61fa-7c92-b13e-4eb5bbd42004"
}
```

只有 `DRAFT` 可删除，操作者必须是上报人或县级全县权限用户。删除为业务逻辑删除，并写入操作轨迹。

> **兼容性警告：**该接口当前使用 `DELETE` + JSON Body，不是 Query 参数。微信客户端、Nginx、API 网关和 WAF 必须进行真实联调，确认请求 Body 不会被丢弃。当前后端没有等价的 Query 参数路由，前端不能自行改成 `?id=` 调用。

### 8.9 直接删除巡查任务

```http
DELETE /river/openapi/v1/inspection/task
Content-Type: application/json
```

权限：`river_inspection`。请求 Body：

```json
{
  "inspectionId": "1930000000000000501",
  "version": 3,
  "requestId": "01980f74-61fa-7c92-b13e-4eb5bbd42005"
}
```

该接口与“删除草稿”的区别是：不限制问题状态，`DRAFT`、`RECTIFYING`、`WITHDRAWN`、`COMPLETED` 均可删除。操作者必须是原上报人或县级全县权限用户。

成功后：

- 逻辑删除巡查问题；
- 逻辑删除该问题关联的待整改或已完成整改任务；
- 清理参与人员快照；
- 若关联月度计划项，且没有其他有效问题占用该计划项，则恢复为 `PENDING`；
- 写入 `DELETE_TASK` 操作轨迹；
- 同一 `requestId` 重试按幂等成功处理。

这是不可在小程序内直接恢复的高风险操作，前端必须二次确认，并在删除成功后退出详情页、刷新问题列表和整改列表。该接口同样使用 `DELETE` + JSON Body，需验证客户端和网关不会丢弃 Body。

---

## 9. 整改任务

### 9.1 整改任务分页

```http
GET /river/openapi/v1/task/page
```

权限：`river_rectification`。

| 参数          | 类型    |   必填 | 说明                     |
| ------------- | ------- | -----: | ------------------------ |
| `current`     | number  | 建议是 | 当前页                   |
| `size`        | number  | 建议是 | 每页条数                 |
| `status`      | string  |     否 | `PENDING` 或 `COMPLETED` |
| `overdueOnly` | boolean |     否 | `true` 只查逾期任务      |

请求：

```http
GET /river/openapi/v1/task/page?current=1&size=10&status=PENDING&overdueOnly=false
```

任务字段 `OpenTaskVO`：

| 字段                          | 类型     | 说明                                |
| ----------------------------- | -------- | ----------------------------------- |
| `id`                          | string   | 整改任务 ID                         |
| `inspectionId`                | string   | 关联问题 ID                         |
| `townshipId` / `townshipName` | string   | 责任乡镇                            |
| `problemLocation`             | string   | 问题地点                            |
| `problemDescription`          | string   | 问题描述                            |
| `deadlineAt`                  | string   | 整改截止时间，`yyyy-MM-dd HH:mm:ss` |
| `rectificationStatus`         | string   | `PENDING`/`COMPLETED`               |
| `description`                 | string   | 已保存的整改说明                    |
| `rectificationImages`         | string[] | 整改图片引用                        |
| `completedBy`                 | string   | 完成人 ID                           |
| `completedAt`                 | string   | 完成时间                            |
| `version`                     | number   | 乐观锁版本                          |
| `overdue`                     | boolean  | 是否逾期                            |

响应示例：

```json
{
  "code": 200,
  "success": true,
  "data": {
    "records": [
      {
        "id": "1930000000000000601",
        "inspectionId": "1930000000000000501",
        "townshipId": "1930000000000000201",
        "townshipName": "桃城镇",
        "problemLocation": "桃城镇某河段桥下",
        "problemDescription": "河道边发现生活垃圾堆放",
        "deadlineAt": "2026-07-23 14:31:00",
        "rectificationStatus": "PENDING",
        "description": "",
        "rectificationImages": [],
        "completedBy": "",
        "completedAt": "",
        "version": 0,
        "overdue": false
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  },
  "msg": "操作成功"
}
```

`OpenTaskVO` 是移动端专用 VO，不继承整改实体。当前不会返回 AI 判断次数、最近 AI 结果、强制完成资格或强制原因。

### 9.2 整改任务详情

```http
GET /river/openapi/v1/task/detail?id={rectificationId}
```

权限：`river_rectification`。

成功响应 `data` 与分页中的 `OpenTaskVO` 字段定义一致。任务按当前县级、乡镇或巡查组数据范围校验；整改处理仍要求责任乡镇权限。

当前任务分页连接关联问题时未过滤问题通用 `status=1`，而非县级用户请求任务详情时会通过关联问题执行要求 `status=1` 的范围校验。因此关联停用问题的任务可能出现在列表中，随后详情被拒绝；前端需兼容并由后端统一查询口径。

### 9.3 提交整改并执行 AI 判定

```http
POST /river/openapi/v1/rectification/complete
Content-Type: application/json
```

权限：有效登录 + `river_rectification_complete`。BladeX 当前按方法级 `@PreAuth` 优先解析，不会再叠加类级 `river_rectification`。

请求字段：

| 字段              | 类型     | 必填 | 说明                                                                                              |
| ----------------- | -------- | ---: | ------------------------------------------------------------------------------------------------- |
| `rectificationId` | string   |   是 | 整改任务 ID                                                                                       |
| `version`         | number   |   是 | 最新版本                                                                                          |
| `requestId`       | string   |   是 | 请求号                                                                                            |
| `description`     | string   |   是 | 整改说明，不能为空；客户端限制不超过 1000 字符，当前 DTO 未做长度校验但数据库列为 `VARCHAR(1000)` |
| `imageRefs`       | string[] |   是 | 整改图片引用，至少 1 个且元素非空                                                                 |

请求：

```json
{
  "rectificationId": "1930000000000000601",
  "version": 0,
  "requestId": "01980f75-6254-7b5a-a3d3-f62a0ec42001",
  "description": "现场垃圾已清理并完成复查",
  "imageRefs": [
    "https://example.com/river/rectification-1.jpg",
    "https://example.com/river/rectification-2.jpg"
  ]
}
```

响应 `data` 为 `RectificationCompletionResult`：

| 字段              | 类型    | 说明                               |
| ----------------- | ------- | ---------------------------------- |
| `rectificationId` | string  | 整改任务 ID                        |
| `status`          | string  | 当前任务状态 `PENDING`/`COMPLETED` |
| `aiResult`        | string  | `COMPLETED`/`INCOMPLETE`/`FAILED`  |
| `aiJudgmentCount` | number  | 已记录的 AI 判断次数               |
| `version`         | number  | 本次处理后的新版本                 |
| `forceRequired`   | boolean | 是否已达到当前后端强制完成阈值     |
| `forceCompleted`  | boolean | 是否通过人工强制完成               |
| `conclusion`      | string  | AI 结论或失败原因/强制原因         |

#### AI 判定完成

```json
{
  "code": 200,
  "success": true,
  "data": {
    "rectificationId": "1930000000000000601",
    "status": "COMPLETED",
    "aiResult": "COMPLETED",
    "aiJudgmentCount": 1,
    "version": 1,
    "forceRequired": false,
    "forceCompleted": false,
    "conclusion": "现场问题已整改完成"
  },
  "msg": "操作成功"
}
```

此时整改任务和关联问题均完成，完成时间由服务器生成。

#### AI 判定未完成

```json
{
  "code": 200,
  "success": true,
  "data": {
    "rectificationId": "1930000000000000601",
    "status": "PENDING",
    "aiResult": "INCOMPLETE",
    "aiJudgmentCount": 1,
    "version": 1,
    "forceRequired": false,
    "forceCompleted": false,
    "conclusion": "现场仍有垃圾残留"
  },
  "msg": "操作成功"
}
```

任务仍待整改。前端必须使用返回的新 `version`，展示 AI 结论并允许用户重新拍摄、重新提交。

#### AI 调用失败或无有效结果

```json
{
  "code": 200,
  "success": true,
  "data": {
    "rectificationId": "1930000000000000601",
    "status": "PENDING",
    "aiResult": "FAILED",
    "aiJudgmentCount": 3,
    "version": 3,
    "forceRequired": true,
    "forceCompleted": false,
    "conclusion": "整改图片 AI 判定未返回有效结果"
  },
  "msg": "操作成功"
}
```

当前实现中 `FAILED` 也会记录一次判断。累计达到 3 次后可能返回 `forceRequired=true`。

> **移动端能力缺口：**后台 Service 和管理端存在“三次未通过后强制完成”能力，但当前移动端没有 `force-complete` 路由；任务详情也不返回累计次数和强制资格。小程序不能虚构强制完成按钮或调用未公开的管理端接口。需要后端补充移动端协议，或明确该操作只能在管理端执行。

真实整改 AI 是否可用取决于运行环境适配器、凭证、阈值和网络。接口代码存在不等于真实模型已联调或准确率已验收。

---

## 10. 通知

接口前缀：

```text
/river/openapi/v1/notification
```

当前 5 个接口均受类级 `river_notification` 权限控制，只能查询或修改当前登录用户自己的通知。

### 10.1 通知分页

```http
GET /river/openapi/v1/notification/page
```

| 参数               | 类型    |   必填 | 说明            |
| ------------------ | ------- | -----: | --------------- |
| `current`          | number  | 建议是 | 当前页          |
| `size`             | number  | 建议是 | 每页条数        |
| `unreadOnly`       | boolean |     否 | `true` 只查未读 |
| `notificationType` | string  |     否 | 按通知类型筛选  |

请求：

```http
GET /river/openapi/v1/notification/page?current=1&size=10&unreadOnly=true&notificationType=TASK
```

通知字段：

| 字段               | 类型    | 说明                            |
| ------------------ | ------- | ------------------------------- |
| `id`               | string  | 通知 ID                         |
| `notificationType` | string  | 通知类型                        |
| `businessType`     | string  | 业务类型                        |
| `businessId`       | string  | 关联业务 ID                     |
| `title`            | string  | 标题                            |
| `content`          | string  | 内容                            |
| `sentAt`           | string  | 发送时间，`yyyy-MM-dd HH:mm:ss` |
| `readAt`           | string  | 阅读时间，未读时为空            |
| `unread`           | boolean | 是否未读                        |

```json
{
  "code": 200,
  "success": true,
  "data": {
    "records": [
      {
        "id": "1930000000000000701",
        "notificationType": "TASK",
        "businessType": "RECTIFICATION",
        "businessId": "1930000000000000601",
        "title": "整改任务分派",
        "content": "收到新的河湖四乱整改任务，请在期限内完成整改。",
        "sentAt": "2026-07-16 14:31:00",
        "readAt": "",
        "unread": true
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  },
  "msg": "操作成功"
}
```

当前服务可能产生的 `notificationType` 包括 `PLAN`、`TASK`、`OVERDUE`、`REPORT`；`businessType` 可能包括 `PLAN`、`PLAN_ITEM`、`RECTIFICATION`、`MONTHLY_REPORT`。这些字段目前是字符串，不是严格封闭枚举。前端遇到未知值时应使用通用通知图标和展示样式。

当前移动端 VO 不返回管理端通知中的 `nodeKey`，不能按 `nodeKey` 实现更细的节点样式或跳转规则。

### 10.2 获取未读通知数

```http
GET /river/openapi/v1/notification/unread-count?notificationType={type}
```

`notificationType` 可选。不传时统计当前用户全部未读通知。

响应：

```json
{
  "code": 200,
  "success": true,
  "data": 3,
  "msg": "操作成功"
}
```

### 10.3 标记单条通知已读

```http
POST /river/openapi/v1/notification/read?id={notificationId}
```

`id` 放在 URL Query 中，不放 JSON Body。只能操作当前用户自己的通知。

### 10.4 全部标记已读

```http
POST /river/openapi/v1/notification/read-all?notificationType={type}
```

`notificationType` 可选。响应 `data` 为本次更新条数：

```json
{
  "code": 200,
  "success": true,
  "data": 5,
  "msg": "操作成功"
}
```

### 10.5 删除本人通知

```http
DELETE /river/openapi/v1/notification/remove?id={notificationId}
```

`id` 放在 URL Query 中。只能删除当前用户自己的通知。

---

## 11. 移动端统计

接口前缀：

```text
/river/openapi/v1/statistics
```

全部接口要求 `river_statistics` 权限，并使用当前 Token 的租户、乡镇和巡查组数据范围。前端传入 `townshipId` 不能扩大服务端范围。

通用筛选参数：

| 参数            | 类型   | 必填 | 说明                                                   |
| --------------- | ------ | ---: | ------------------------------------------------------ |
| `from`          | string |   否 | 开始日期，`yyyy-MM-dd`；默认本月 1 日                  |
| `to`            | string |   否 | 结束日期，`yyyy-MM-dd`；默认当天                       |
| `townshipId`    | string |   否 | 乡镇 ID；县级用户可筛选，非县级仍以 Token 数据范围为准 |
| `problemTypeId` | string |   否 | 问题大类 ID                                            |

`from` 不能晚于 `to`，查询跨度不能超过 366 天。

### 11.1 统计总览

```http
GET /river/openapi/v1/statistics/overview?from=2026-07-01&to=2026-07-31
```

响应 `data`：

```json
{
  "issuedCount": 28,
  "completedCount": 20,
  "overdueCount": 3,
  "rectificationRate": 71.43,
  "overdueRate": 10.71
}
```

### 11.2 乡镇汇总

```http
GET /river/openapi/v1/statistics/township-summary?from=2026-07-01&to=2026-07-31
```

返回数组字段：`townshipId`、`townshipName`、`issuedCount`、`completedCount`、`overdueCount`、`rectificationRate`、`overdueRate`。

### 11.3 月度趋势

```http
GET /river/openapi/v1/statistics/trend?from=2026-01-01&to=2026-07-31
```

返回数组字段：`month`、`issuedCount`、`completedCount`。

### 11.4 问题类型分布

```http
GET /river/openapi/v1/statistics/problem-distribution?from=2026-07-01&to=2026-07-31
```

返回数组字段：`problemTypeId`、`problemTypeName`、`problemCount`。

### 11.5 逾期问题明细

```http
GET /river/openapi/v1/statistics/overdue-cases?from=2026-07-01&to=2026-07-31&limit=50
```

`limit` 默认 `50`，服务端限制在 `1~200`。返回数组字段：`inspectionId`、`rectificationId`、`townshipName`、`location`、`deadlineAt`、`completedAt`。

### 11.6 月度计划进度

```http
GET /river/openapi/v1/statistics/plan-progress?month=2026-07&townshipId={townshipId}
```

| 参数         | 类型   | 必填 | 说明                  |
| ------------ | ------ | ---: | --------------------- |
| `month`      | string |   否 | `yyyy-MM`，默认当前月 |
| `townshipId` | string |   否 | 乡镇 ID               |

响应 `data`：

```json
{
  "planMonth": "2026-07",
  "totalCount": 22,
  "completedCount": 18,
  "completionRate": 81.82
}
```

---

## 12. 常见业务错误与前端处理

精确业务码和 HTTP 状态以运行环境为准，前端优先根据 `msg` 给出用户可理解的提示。

| 可能的 `msg`                               | 建议处理                         |
| ------------------------------------------ | -------------------------------- |
| `乡镇不能为空`                             | 定位乡镇字段                     |
| `巡查参与人员至少需要2人`                  | 提示选择至少 2 名有效人员        |
| `巡查参与人员包含无效用户`                 | 刷新参与人员候选数据             |
| `经度和纬度必须同时填写或同时留空`         | 清空两项或补齐另一项             |
| `经度不能小于-180` / `经度不能大于180`     | 校验经度范围                     |
| `纬度不能小于-90` / `纬度不能大于90`       | 校验纬度范围                     |
| `所选河段不属于当前乡镇`                   | 清空河段并重新选择               |
| `巡查计划明细与所选乡镇不一致`             | 根据计划项回填乡镇               |
| `巡查计划明细不存在或尚未发布`             | 刷新计划项选项                   |
| `无权使用该巡查计划明细`                   | 提示数据范围不足                 |
| `问题大类无效`                             | 刷新问题类型选项                 |
| `问题子类不属于所选大类`                   | 清空子类并重新选择               |
| `只有草稿问题可以修改`                     | 刷新详情，切换只读状态           |
| `乡镇、位置和问题描述不能为空`             | 补齐提交必填项                   |
| `现场背景图片、问题图片和问题类型不能为空` | 分别上传背景图、问题图并选择类型 |
| `只有草稿问题可以提交`                     | 禁止重复提交并刷新状态           |
| `只有整改中的问题可以撤回`                 | 刷新问题状态                     |
| `乡镇已提交整改，问题不能撤回`             | 禁用撤回，刷新整改详情           |
| `只有已撤回的问题可以恢复为草稿`           | 刷新问题状态                     |
| `只有草稿问题可以删除`                     | 禁用删除并刷新状态               |
| `只能操作本人上报的问题`                   | 提示没有操作权限                 |
| `巡查问题不存在`                           | 返回列表并刷新                   |
| `图片识别失败，请手工填写`                 | 保留人工填写流程，不阻断草稿保存 |
| `整改任务不存在`                           | 返回任务列表并刷新               |
| `只有待整改任务可以处理`                   | 停止重复提交并刷新任务           |
| `整改说明和整改图片不能为空`               | 补齐说明和图片                   |
| `整改任务已被其他用户修改，请刷新后重试`   | 获取最新详情和版本               |
| `巡查问题状态已变化，请刷新后重试`         | 同时刷新问题和任务               |
| `无权访问其他乡镇数据`                     | 提示权限不足，不展示越权数据     |
| `请求过于频繁，请稍后再试`                 | 延迟重试，禁止高频循环           |

所有校验错误都应保留用户已输入的表单内容；不要因一次接口失败清空图片引用和描述。

---

## 13. 推荐小程序调用流程

### 13.1 应用启动

```text
账号密码登录或刷新取得 BladeX Token
  → GET /profile
  → 保存用户资料和数据范围
  → 按角色与菜单权限展示问题、整改、通知入口
```

前端入口控制只用于体验优化，服务端权限仍是最终依据。

### 13.2 上报问题

```text
加载乡镇/河段/问题类型/计划项
  → 从待补充的安全来源获取参与人员
  → 拍摄并分别上传背景图、问题图
  → 可选：问题图 AI 识别
  → 用户人工确认类型、位置和描述
  → 保存草稿，取得 inspectionId
  → 请求详情，取得最新 version
  → 生成全局唯一 requestId
  → 提交问题
  → 刷新问题列表/详情
```

### 13.3 撤回并重新提交

```text
获取详情和最新 version
  → 撤回问题
  → 刷新详情
  → 恢复为草稿
  → 刷新详情
  → 编辑并保存草稿
  → 再次刷新 version
  → 使用新的 requestId 重新提交
```

### 13.4 提交整改

```text
获取任务详情和最新 version
  → 上传整改图片
  → 填写整改说明
  → 生成 requestId
  → POST /rectification/complete
  → COMPLETED：提示完成并刷新列表
  → INCOMPLETE：展示 AI 结论，保存返回的新 version，允许重试
  → FAILED：提示 AI 失败，保存返回的新 version，允许按业务规则处理
  → forceRequired=true：提示需转管理端/等待移动端协议补齐
```

### 13.5 通知

```text
获取 unread-count 展示红点
  → 分页加载通知
  → 打开通知后标记单条已读
  → 支持按类型全部已读
  → 用户确认后删除本人通知
```

### 13.6 统计

```text
按页面选择日期、乡镇和问题类型
  → 并行加载 overview、trend、problem-distribution
  → 需要明细时加载 township-summary 或 overdue-cases
  → 计划页按 yyyy-MM 加载 plan-progress
  → 筛选变化时取消旧请求，避免旧响应覆盖新筛选结果
```

### 13.7 防重复提交

- 点击写操作按钮后立即进入 loading；
- 同一请求未结束前禁止重复点击；
- 问题提交、撤回、恢复和删除因网络超时重试时复用原 `requestId`；
- 用户明确发起新的业务操作时生成新 `requestId`；
- 整改提交的 `INCOMPLETE`/`FAILED` 分支没有持久化 `requestId`，超时后先刷新任务并提示用户确认，不得自动重放；
- 如果任务已完成则停止重试；如果仍待整改，也要把再次提交视为可能再次调用 AI、增加判断次数的操作。

---

## 14. 当前边界与正式联调阻塞

| 序号 | 项目                   | 当前状态/处理要求                                                     |
| ---: | ---------------------- | --------------------------------------------------------------------- |
|    1 | 测试、生产 Base URL    | 待部署方提供，生产必须 HTTPS                                          |
|    2 | 微信登录和账号绑定     | 当前无 `wx.login` code 登录；现阶段使用 BladeX 账号密码登录           |
|    3 | Token 获取、刷新、注销 | 已有 BladeX OAuth2 接口，仍需真实小程序客户端联调                     |
|    4 | 小程序 OAuth Client    | 尚未确认；禁止复用并暴露后台 `clientSecret`                           |
|    5 | 参与人员候选           | 当前 OpenAPI 缺失，但保存草稿强制至少 2 人                            |
|    6 | 移动端整改强制完成     | 后台 Service 已有，移动端路由和任务字段缺失                           |
|    7 | `capturedAt` JSON 格式 | 待运行环境验证；不可把 GET 格式直接套用                               |
|    8 | DELETE JSON Body       | 需微信客户端、Nginx、网关/WAF 实测                                    |
|    9 | OSS 文件策略           | 大小、格式、权限、生命周期、孤儿清理待确认                            |
|   10 | 微信合法域名           | `request`、`uploadFile`、`downloadFile` 待配置                        |
|   11 | 问题图 AI              | 接口已存在；真实适配器、凭证、标签、阈值、SLA 待确认                  |
|   12 | 整改图 AI              | 流程已存在；真实模型、阈值、样例和准确率待验收                        |
|   13 | 真实人员与账号         | 业务资料为 4 组/9 人；真实用户 ID、组归属待提供                       |
|   14 | 正式问题类型           | DOCX 与原型简化分类存在差异，需业务确认                               |
|   15 | 小程序源码             | 当前项目没有可发布的小程序工程                                        |
|   16 | 计划项状态一致性       | 直接 ID 校验和提交未复核计划项仍为 `PENDING`，存在重复上报风险        |
|   17 | 停用乡镇链路           | 停用乡镇仍可能经计划项进入上报，但普通乡镇用户无法整改                |
|   18 | 需求矩阵               | `requirement-coverage-matrix.md` 的部分状态落后当前工作树，需另行同步 |

每月 5 日上月汇总、地图定位/逆地理编码、统计对话等业务要求不在当前 28 个移动端路由内。普通移动端统计查询已提供，但不能用后台已有能力或 HTML 原型替代小程序接口和真机验收。

---

## 15. 联调与验收清单

### 15.1 环境与鉴权

- [ ] 测试/生产 Base URL 已提供且使用 HTTPS；
- [ ] `request`、`uploadFile`、`downloadFile` 合法域名已配置；
- [ ] Token 获取、刷新、过期、注销流程已验证；
- [ ] 请求包含 `Blade-Auth` 和 `Blade-Requested-With`；
- [ ] 未登录、无菜单权限请求被拒绝；
- [ ] 小程序代码中不存在后台 `clientSecret` 和微信 AppSecret。

### 15.2 契约

- [ ] 28 个 OpenAPI 路由逐项核对方法、路径和参数位置；
- [ ] 所有 Long ID 按字符串处理；
- [ ] GET 时间参数按 `yyyy-MM-dd HH:mm:ss` 编码；
- [ ] `capturedAt` JSON 格式已用真实环境确认；
- [ ] 分页空列表、空详情和未知通知类型已兼容；
- [ ] `DELETE /inspection/draft` 的 JSON Body 未被客户端或网关丢弃。
- [ ] `DELETE /inspection/task` 的 JSON Body 未被客户端或网关丢弃。

### 15.3 权限与数据范围

- [ ] 县级管理员、巡查员、乡镇用户分别使用真实 Token 验证；
- [ ] 篡改 `townshipId`、`sectionId`、`planItemId`、问题 ID、任务 ID 不能越权；
- [ ] 非上报人不能编辑、提交、撤回、恢复或删除他人问题，县级策略除外；
- [ ] 乡镇用户不能完成其他乡镇任务；
- [ ] 通知接口只能操作当前用户通知。

### 15.4 问题流程

- [ ] 参与人员安全来源已解决，去重后至少 2 人；
- [ ] 背景图和问题图分别上传和保存；
- [ ] 经纬度全空、成对合法、单项缺失、越界四类场景已验证；
- [ ] 问题类型父子关系和河段—乡镇关系已验证；
- [ ] 提交前刷新计划项，验证已完成计划项及同一计划项多草稿不会重复生成任务；
- [ ] 停用乡镇不会继续出现在计划项并形成无人可处理的整改任务；
- [ ] 保存草稿后重新获取最新 `version`；
- [ ] 提交、撤回、恢复、删除和撤回重提全链路已验证；
- [ ] 各问题状态直接删除任务后，问题、整改任务和计划项状态一致；
- [ ] 旧 `version` 冲突不会覆盖他人修改；
- [ ] 同一 `requestId` 重试和不同业务 requestId 唯一性已验证。

### 15.5 整改与 AI

- [ ] 整改图片、说明必填；
- [ ] AI `COMPLETED`、`INCOMPLETE`、`FAILED` 三个分支均已验证；
- [ ] 未完成/失败后采用响应中的新 `version`；
- [ ] 达到 3 次后的移动端处理策略已确认；
- [ ] 真实 AI 未配置时，前端有明确提示且不会误报完成；
- [ ] 整改请求超时后先刷新状态再决定是否重试。

### 15.6 通知、统计与弱网

- [ ] 通知分页、类型筛选、未读数、单条已读、全部已读、删除均已验证；
- [ ] 6 个统计接口的默认日期、筛选、数据范围和 366 天限制均已验证；
- [ ] 未知通知类型使用通用样式；
- [ ] 弱网、超时、断网恢复和重复点击已验证；
- [ ] 上传成功但业务保存失败时有用户提示和清理策略。

---

## 16. Knife4j 与验证边界

Knife4j 通常可通过以下地址访问，实际路径以部署环境为准：

```text
{baseUrl}/doc.html
```

本文已按当前工作树进行静态源码核对，但不等同于真实环境联调通过。现有 `RiverOpenApiContractTest` 不能替代 28 个接口的真实环境验证。正式交付前仍需通过 Knife4j、真实 Token、三类角色、OSS、网关和真机完成端到端验证。

## 智能统计接口

http://192.168.2.103:8201/integrated/stream/chat/chat
post 请求
{"sessionId":"20260716-1631-iju2gj","content":"古树未巡查地区数量统计","modelId":"2039881758544191489","mcpIds":[],"sqlDbIds":["2070086245887369218"],"knowledgeBaseIds":["2070337973521551361","2061984569413406722"],"toolIds":[],"frontendSkillIds":["openResourceDetail","openPolicyDetail","locateResource"],"fileUrlList":[]}
